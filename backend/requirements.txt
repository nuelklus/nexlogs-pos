# Core dependencies
setuptools<82.0.0
wheel>=0.40.0
packaging>=24.0

# Core Django and DRF
Django==5.0.7
djangorestframework==3.15.2
djangorestframework-simplejwt>=5.3.1
django-filter==23.5

# Database
psycopg2-binary==2.9.12
dj-database-url==2.2.0

# CORS and Environment
django-cors-headers==4.4.0
python-dotenv==1.0.1

# Production server and static files
gunicorn==22.0.0
whitenoise==6.7.0

# Email (optimized for production)
resend==0.8.0

# Image processing and Supabase storage
Pillow==10.4.0
supabase==2.29.0

# Security and optimization (minimal for 512MB RAM)
# Removed: django-debug-toolbar, django-extensions

# Development tools (commented out for production)
# django-debug-toolbar==4.2.0
# django-extensions==3.2.3
